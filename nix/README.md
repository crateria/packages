# Nix / NixOS Expressions

This directory contains Nix derivations and Flakes for IdleScreen:

- `default.nix`: Standard nix-build expression
- `flake.nix`: Nix Flake definition

## Building with Nix

```bash
nix-build nix/default.nix
```
