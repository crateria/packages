# Arch Linux / AUR Packages

This directory contains Arch Linux package definitions (`PKGBUILD`) for IdleScreen components:

- **`idle-daemon`**: Core Wayland screensaver daemon
- **`idle-cosmic`**: COSMIC Desktop applet
- **`idle-cli`**: Command-line controller

## Building with makepkg

```bash
cd arch/
makepkg -si
```
